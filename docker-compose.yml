version: '3.8'

services:
  receipts:
    build: .
    container_name: receipts-processor
    volumes:
      - ./imgs:/app/imgs
      - ./.env:/app/.env
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped
    stdin_open: true
    tty: true
